<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:locals="clr-namespace:Exam"
             xmlns:views="clr-namespace:Exam.Views"
             xmlns:models="clr-namespace:Exam.Models"
             x:Class="Exam.Views.Modals.ExerciseModal"
             x:Name="This"
             BindingContext="{x:Reference This}">

    <StackLayout Style="{DynamicResource PageWrapper}" Padding="{StaticResource SpacingLarge}">
        <Label Text="Titel" />
        <Entry Text="{Binding Exercise.Title}" />
        <Label Text="Beskrivelse" />
        <Entry Text="{Binding Exercise.Description}" />
        <Button Text="{Binding ButtonText}" Clicked="Create" />
    </StackLayout>
</ContentPage>
