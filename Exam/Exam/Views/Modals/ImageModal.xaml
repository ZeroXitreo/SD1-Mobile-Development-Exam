<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:locals="clr-namespace:Exam"
             xmlns:views="clr-namespace:Exam.Views"
             xmlns:models="clr-namespace:Exam.Models"
             x:Class="Exam.Views.Modals.ImageModal"
             x:Name="This"
             BindingContext="{x:Reference This}"
             Title="Ny øvelse">
    <StackLayout Style="{DynamicResource PageWrapper}" Padding="{StaticResource SpacingLarge}">
        <Button Text="Billede fra kamera" Clicked="ImageFromCamera" />
        <Button Text="Billede fra storage" Clicked="ImageFromStorage" />
        <Image x:Name="ImagePreview" />
        <Button Text="Save" Clicked="UploadImage" />
    </StackLayout>
</ContentPage>